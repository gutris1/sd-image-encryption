# SD Encrypt Image

Image encryption in Stable Diffusion WebUI, only necessary if you need to avoid something.
add --encrypt-pass=your_password argument to the command line.

check release page, to decrypt images in windows.
